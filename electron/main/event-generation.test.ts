import assert from "node:assert/strict"; import test from "node:test"; import type { EventSummary } from "../shared/contracts.js"; import { buildEventCampaign, buildEventPrompt, generateEventCopy } from "./event-generation.js";
const event:EventSummary={id:"event-1",kind:"ORGANIZER",name:"Sonic Ark Night",status:"PLANNED",date:"2026-10-24",doorsTime:"20:00",startTime:"22:00",endTime:null,timezone:"Europe/Berlin",venue:"Club Test",address:"Berlin",genre:"Psytrance",description:"A label night",ticketUrl:null,eventUrl:null,socialLinks:[],organizer:"Sonic Ark Records",createdAt:"",updatedAt:"",lineup:[{id:"slot",eventId:"event-1",artistName:"The Arkadiusz",performanceStart:"23:00",performanceEnd:null,sortOrder:0}],assets:[],campaignProgress:0};
test("event prompt is grounded and forbids invented commercial facts",()=>{const prompt=buildEventPrompt(event,{eventId:event.id,contentType:"TICKET_PROMOTION",language:"pl",platform:"Facebook",tone:"direct"});assert.match(prompt,/Never invent prices/);assert.match(prompt,/Ticket URL: not provided/);assert.match(prompt,/The Arkadiusz/);});
test("campaign milestones are deterministic and relative to event date",()=>{const first=buildEventCampaign(event,{eventId:event.id,language:"en",platforms:["Instagram","Facebook"],tone:"clear"}),second=buildEventCampaign(event,{eventId:event.id,language:"en",platforms:["Instagram","Facebook"],tone:"clear"});assert.deepEqual(first,second);assert.equal(first.length,14);assert.ok(first.every(item=>item.status==="DRAFT"&&item.scheduledAt));assert.equal(first.at(-1)?.contentType,"POST_EVENT_THANK_YOU");});
test("provider failure does not produce copy",async()=>{await assert.rejects(()=>generateEventCopy(event,{eventId:event.id,contentType:"EVENT_ANNOUNCEMENT",language:"de",platform:"Instagram",tone:"clear"},async()=>({ok:false,durationMs:1,attempts:[],errorMessage:"offline"})),/no usable content/);});
